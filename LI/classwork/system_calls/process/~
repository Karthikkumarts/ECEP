#include<stdio.h>
#include<stdlib.h>
int main()
{
    char str[20];
    printf("enter the command to exceute from system:");
    scanf("%s",str);
    printf("running the %s command using the system\n",str);
    system(str);
    printf("DONE\n");
}
